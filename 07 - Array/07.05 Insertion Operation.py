class Array:
    '''
     Insertion
      arr[index] = value
      Insert at beginning
      Move all elements one to right and insert at start
      What if array is full
      we need to create a new array and copy then perform the operation
      Time - O(N) at beginning or middle and O(1) at end
      Space - O(1)
    '''


if __name__ == "__main__":
    print(Array().__doc__)