class Hashing:
    '''
    Hashing Terminology
      Hash function - It is a function that can be used to map an arbitrary size to data of fixed size
      Key: Input data by a user
      Hash value - A value that is returned by the hashing function
      Hash table - It is a data structure which implements an associative array abstract data type, a structure that can map keys and values
      Collision - A collision occurs when two different keys to a hashfunction produces same output
    '''


if __name__ == "__main__":
    print(Hashing().__doc__)