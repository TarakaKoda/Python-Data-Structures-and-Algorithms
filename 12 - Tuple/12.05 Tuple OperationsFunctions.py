class Tuple:
    '''
    Tuple Operations and methods
    + : concatenation
    * : repeat add
    in: search
    count(): gives count of an element
    len(): gives the length of tuple
    max() - gives maximum of the tuple
    min() - gives minimum of the tuple
    tuple() - used to convert a list into tup
    '''


if __name__ == "__main__":
    print(Tuple().__doc__)