class Dictionary:
    '''
      Dictionary Methods:
      clear()
      cop()
      fromkeys(sequence[],value) - new dictionary
      get(key,value)
      items()
      keys()
      popitem()
      setDefault(key,default_value)
      pop(key,default_value)
      values()
      update(other_dict)
    '''

if __name__ == "__main__":
    print(Dictionary().__doc__)