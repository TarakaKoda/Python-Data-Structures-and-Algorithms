class Dictionary:
    '''
      Insert/Update an element in the dictionary
      dict['key'] = 'value' It will update if the key already exists or adds the new key value to the dictionary if it
      doesn't already exist.
      Time - O(1)
    '''

if __name__ == "__main__":
    print(Dictionary().__doc__)